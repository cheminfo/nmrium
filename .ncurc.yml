dep: 'prod,dev'
reject:
  # https://github.com/cheminfo/nmrium/issues/3983
  - '@playwright/test'
  # Our plugins are not all compatible with v10
  - eslint
  # Types are deeply broken between this package and nmrium
  - nmr-correlation
  # We're not ready for React 19 yet
  - react
  - react-dom
  - '@types/react'
  - '@types/react-dom'
